# Fonts

One UI typography stack used by the interface:

- `samsungone_400.ttf`, `samsungone_700.ttf` - body text (`font-samsung`)
- `samsungsharpsans_medium.otf`, `samsungsharpsans_bold.otf` - headings (`font-sharp`)
