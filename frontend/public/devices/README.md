Place device PNG images here

Examples:
- Firmware cache cards and top status buttons (model-based, normalized):
  - `SM-S908B` / `SM-S908E` / `SM-S908N` -> `SM-S908.png`
  - `SM-S901B` / `SM-S901E` -> `SM-S901.png`

The UI loads:
- `/devices/<normalized-model>.png` for firmware cards and top status bar

If image is missing, it falls back to a generated placeholder icon
